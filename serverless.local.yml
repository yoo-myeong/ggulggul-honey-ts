service: gh
frameworkVersion: '3'

custom:
  serverless-offline:
    httpPort: 4000

provider:
  name: aws
  runtime: nodejs18.x
  environment:
    NODE_ENV: local

functions:
  api:
    handler: ./dist/src/customer-customer-customer-api/index.handler
    events:
      - httpApi: '*'

plugins:
  - serverless-offline

layers:
  libs:
    path: ./dist/libs
    name: libs
    retain: false
